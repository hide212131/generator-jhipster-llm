node {
    download = true
}

task downloadLLMModel(type: NpmTask) {
    args = ['run', 'llm:download-model', '--', "${project.findProperty('llm.model.home')}", project.findProperty('llm.model.name'), project.findProperty('llm.model.url')]
}

processResources.dependsOn downloadLLMModel